/**
 * @sage/core — Chain-agnostic types, interfaces, and protocol invariants for Sage.
 *
 * This package has zero runtime dependencies and must not import
 * any chain-specific libraries (viem, ethers, anchor, etc.).
 */

export const SAGE_PROTOCOL_VERSION = '2.0.0' as const;
