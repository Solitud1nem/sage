{
  "name": "@sage/worker-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker fronting the Sage demo backend: RPC proxy (Alchemy key hidden) + rate-limited passthrough to the Fly.io orchestrator.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute sage-rate-limits --file=./schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250110.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.99.0"
  }
}
